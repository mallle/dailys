{% extends 'base.html.twig' %}

{% block title %}All your habits{% endblock %}

{% block main %}

    <div class="d-flex justify-content-between p-2">
        <h1>All Goals</h1>
        <div class="d-">
            <a href="{{ path('app_goals_new')}}" class="btn btn-sm btn-outline-secondary">New</a>
        </div>
    </div>
    {% if goals|length > 0 %}
        <div class="table-responsive">
            <table class="table table-striped table-sm">
                <thead>
                <tr>
                    <th>Name</th>
                    <th class="text-center">Action</th>
                </tr>
                </thead>
                <tbody>
                {% for goal in goals %}
                    <tr>
                        <td>{{ goal.name }}</td>
                        <td class="text-center">
                            <a href="{{ path('app_goals_edit', { goal: goal.id })}}" class="btn btn-sm btn-outline-secondary">Edit</a>
                            <a href="{{ path('app_goals_delete', { goal: goal.id })}}" class="btn btn-sm btn-outline-secondary">Delete</a>
                            <a href="{{ path('app_goals_done', { goal: goal.id })}}" class="btn btn-sm btn-outline-secondary">Done</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    {% else %}
        <p>Add a new Goals before you can manage and track them 😁</p>
    {% endif %}

{% endblock %}
