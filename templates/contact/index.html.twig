{% extends 'base.html.twig' %}

{% block title %}Contact us!{% endblock %}

{% block main %}
    <div class="d-flex justify-content-center">
        <div class="col-lg-8">
            <h1>
               Contact us
            </h1>
            <div class="pt-3">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
