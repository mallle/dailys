{% macro isChecked(obmonthHabitj, month) %}
{#    {% for monthHabitToDay in monthHabit.monthHabitToDays %}#}
{#        {% if monthHabitToDay.isChecked %}#}
{#            {% if monat monthHabitToDay.monat %}#}
{#            button-checked#}

{#            {% endif %}#}
{#        {% endif %}#}
{#    {% endfor %}#}
{% endmacro %}


{% macro activeNavi(val_current, val_check, index = 0, class = 'active') %}
    {% if val_current is iterable %}
        {% if (val_current[index] == val_check) %}{{ class }}
        {% endif %}
    {% elseif val_check is iterable %}
        {% if val_current in val_check %}{{ class }}
        {% endif %}
    {% else %}
        {% if (val_current == val_check) %}{{ class }}
        {% endif %}
    {% endif %}
{% endmacro %}